// compare two arrays and return a new array with any items only found in one of the given arrays, but not both. Return the symmetric difference. 

function diffArray(arr1, arr2) {
    
}

module.exports = {
    diffArray
}